/**
 * Base application-layer context for the InstallApp use-case pipeline.
 *
 * - Always an object
 * - Intentionally empty
 * - Applications may extend this with their own fields
 */
export type InstallAppContext = Record<string, unknown>;
