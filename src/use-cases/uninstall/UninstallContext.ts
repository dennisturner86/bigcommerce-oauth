/**
 * Base application-layer context for the Uninstall use-case pipeline.
 *
 * - Always an object
 * - Intentionally empty
 * - Applications may extend this with their own fields
 */
export type UninstallContext = Record<string, unknown>;
