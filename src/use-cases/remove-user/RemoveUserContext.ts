/**
 * Base application-layer context for the RemoveUser use-case pipeline.
 *
 * - Always an object
 * - Intentionally empty
 * - Applications may extend this with their own fields
 */
export type RemoveUserContext = Record<string, unknown>;
